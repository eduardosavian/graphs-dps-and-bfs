#ifndef PLOT_CPP
#define PLOT_CPP

#include "plot.hpp"

void plotGraphMatrix() {
    // print the graph using a nxn matrix

    // int n = vertices.size();
    // std::cout << "n: " << n << "\n";
    // int matrix[n][n];
    // for (int i = 0; i < n; i++) {
    //     for (int j = 0; j < n; j++) {
    //         matrix[i][j] = 0;
    //     }
    // }

    // for (uint i = 0; i < edges.size(); i++) {
    //     matrix[edges[i].first - 1][edges[i].second - 1] = 1;
    //     matrix[edges[i].second - 1][edges[i].first - 1] = 1;
    // }

    // std::cout << "Graph: \n";
    // for (int i = 0; i < n; i++) {
    //     for (int j = 0; j < n; j++) {
    //         std::cout << matrix[i][j] << " ";
    //     }
    //     std::cout << "\n";
    // }

}

#endif